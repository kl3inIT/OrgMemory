# Plan

- [ ] Add deterministic local Skill folder inspection and `skill validate`.
- [ ] Add the OAuth `assets:write` scope and a bounded publication companion
  endpoint without adding MCP mutation tools.
- [ ] Add `skill publish` with dry-run and machine-readable output.
- [ ] Reuse canonical Skill Draft creation and governance without a parallel
  lifecycle.
- [ ] Add focused CLI, gateway, security, and deployment-contract tests.
- [ ] Run backend, CLI, generated-contract, deployment, and static-analysis
  gates.
- [ ] Open the pull request and resolve CI/review findings.
