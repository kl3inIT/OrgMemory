# Knowledge Workspace And Document Reader Plan

- [x] Audit the current Documents information hierarchy, evidence reader,
  presentation allowlist, authorization copy, and browser coverage.
- [ ] Rename the visible workspace to Knowledge while retaining Documents and
  Knowledge graph as peer tabs on `/sources`.
- [ ] Extract the restricted Markdown renderer to a shared product pattern and
  render declared Markdown safely with a Raw fallback.
- [ ] Make the right-side reader fill the viewport, add retry, and preserve
  exact inline/download-only format boundaries.
- [ ] Replace inferred audience copy with the governing Knowledge Space policy.
- [ ] Extend unit and Playwright coverage across presentation kinds and
  responsive behavior.
- [ ] Run completion gates, reconcile the domain spec/test pair, add release
  evidence, and complete the increment.
