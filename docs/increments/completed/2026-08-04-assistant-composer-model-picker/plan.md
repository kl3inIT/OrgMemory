# Assistant Composer And Conversation Model Picker Plan

- [x] Complete the independent architecture challenge and incorporate every
  must-fix into the design.
- [x] Add the administrator-owned Assistant model catalog and tenant-safe
  persistence contract.
- [x] Add actor-owned conversation model selection and exact-route generation
  with route-change semantics.
- [x] Generate the REST client and compose the AI Elements model selector into
  the Assistant prompt input.
- [x] Remove `Permission-aware` and refine empty-state hierarchy, spacing, and
  starter presentation from verified reference patterns.
- [x] Add focused backend, frontend, accessibility, and browser interaction
  coverage.
- [x] Reconcile the Assistant spec and coverage matrix, run completion gates,
  and drive the PR/CI/review loop through merge.
