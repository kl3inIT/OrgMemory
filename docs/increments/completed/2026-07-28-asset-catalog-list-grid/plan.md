# Plan

1. [x] Add `releasedAt` to the catalog projection and refresh the API contract.
2. [x] Add URL-backed list/grid state with list as the default.
3. [x] Build the default catalog with the shared TanStack `DataTable`.
4. [x] Compact the grid cards and replace the Work Instruction icon.
5. [x] Let shared pagination show a range summary on a single page.
6. [x] Add focused backend and frontend tests for the new stable behavior.
7. [x] Run static analysis fallbacks, backend tests, frontend
   lint/typecheck/unit/build, Edge E2E, and design QA.
8. [ ] Open a PR, resolve CI/review findings, merge, and verify automatic
   deployment.
