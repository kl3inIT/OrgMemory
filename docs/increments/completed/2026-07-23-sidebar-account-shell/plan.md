# Plan

1. Move the desktop collapse control into both workspace and admin sidebars.
2. Move administration, account, theme, and sign-out controls into sidebar
   footers.
3. Replace the global header with a mobile-only navigation bar.
4. Add consistent search, filters, result ranges, and pagination to permission
   collections; collapse source-group membership until requested.
5. Run Oxlint, TypeScript, production build, and authenticated browser checks
   for workspace and admin routes in light and dark themes.
